fastapi==0.110.0
uvicorn[standard]==0.27.0
pydantic==2.6.0
python-dotenv==1.0.0

# LangChain 生态
langchain==0.1.12
langchain-openai==0.0.8

# Milvus 客户端
pymilvus==2.4.1

# 常用工具
httpx==0.27.0
tenacity==8.2.3

# Redis 缓存（embedding / RAG 检索）
redis==5.0.4

# G-5.2：Langfuse 全链路 trace SDK（v2 系列，对应 self-hosted langfuse:2 server）
# 三个环境变量缺失时 SDK 自动 no-op，不影响主流程
langfuse>=2.36,<3.0